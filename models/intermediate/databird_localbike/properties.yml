version: 2

models:
  - name: int_databird_localbike__manager
    columns:
      - name: manager_id
        description: "Primary key of this table"
        tests:
          - unique
          - not_null

  - name: int_databird_localbike__order
    columns:
      - name: order_id
        description: "Primary key of this table"
        tests:
          - unique
          - not_null

  - name: int_databird_localbike__order_items
    columns:
      - name: order_item_id
        description: "Primary key of this table"
        tests:
          - unique
          - not_null
    
  - name: int_databird_localbike__products
    columns:
      - name: product_id
        description: "Primary key of this table"
        tests:
          - unique
          - not_null
    
  - name: int_databird_localbike__stocks
    columns:
      - name: stock_id
        description: "Primary key of this table"
        tests:
          - unique
          - not_null